# 更新日志

## 1.6.0

- **手动通知支持自定义标题、头像、直播封面**：点「手动发开播／下播」后可在确认面板里单独指定，
  支持粘贴 https 图片直链或上传本地图片（≤2MB，转 data URI 内联进卡片）。
- **默认继承上次手动发送的素材**，表单会预填；从未手动发送过则为空白，并在占位符里显示当前
  在线检测到的标题／头像／封面，同时给出「留空即使用上次手动发送的素材；若从未手动发送过，
  则使用在线检测到的直播间资料」的说明。
- **手动与在线检测严格隔离**：手动素材只作用于那一次发送，不写回 `states`／`rooms`；
  在线检测的通知始终使用 B 站返回的真实标题、头像与封面。发送记录里固化当次使用的素材，
  因此「仅补发此群」会沿用当时的素材，不会串成当前配置。
- 手动素材的图片只接受图片类型的 data URI（正则校验、限 3MB 字符），拒绝 SVG 与其它 MIME，避免注入。
- 新增 7 项测试：自定义素材生效与记忆、留空回退且不改动检测状态、自动路径不受手动配置影响、
  补发沿用当时素材、WebUI 表单渲染与预填、空态提示、提交载荷。

## 1.5.5

- **修复头像被裁掉头顶**：之前头像向图床请求时加了 `1c`（居中裁剪成正方形），而头像源图本来就是 1:1，裁切只会吃掉边角；又因为图床按容器尺寸被 `object-fit: cover` 放大，圆形遮罩再切一圈，头顶就被切掉了。
  - 头像缩略图从 160px 提到 240px（卡片内以 96 CSS px 显示，1.8 倍高分屏需要约 173px），不再拉伸模糊。
  - 去掉多余裁剪，头像铺满整个圆形（用白色描边代替原来的内边距，视觉不变、有效直径更大）。
- **新增「头像取景位置」配置**：头像是圆形显示，方图的四角必然被切。人物头顶被切或偏下时，可在「直播订阅 → 通知卡片」切换取景位置（偏上／居中／偏下／上部三分之一／下部三分之一），默认偏上。取值做了白名单校验，不会注入样式。
- 新增 1 项测试：头像 URL 不得包含裁切参数、取景位置要写入卡片样式、非法取值被拒绝。

## 1.5.4

- 文档示例截图从 PNG 改为 JPEG：三张图从 1092KB 压到 233KB，安装包从 1198KB 降到约 350KB。功能无变化。
- 顺带记录包体构成：运行代码（lib + webui + assets）约 300KB，其余为文档与示例图。

## 1.5.3

- **修复卡片里没有头像和直播封面**：插件复用的 B站请求通道有两道限制——域名白名单只放行 `bilibili.com` / `b23.tv`（那是短链跳转校验），且无条件把响应体按 JSON 解析。封面与头像在图床 `*.hdslb.com` 上，于是每次取图都被拒绝，卡片只能退回默认的「爱」字头像和空封面。
  - 请求通道新增 `allowedHosts`：只放行 `hdslb.com` 及其子域，仍然禁止 http、端口、用户名密码（有测试防子域欺骗）。
  - 请求通道新增 `raw`：二进制资源直接返回字节流，不再尝试 JSON 解析。
- **开播／下播文案现在带可点击链接**：开播文案以「🔴 正在直播 → 链接」开头，下播文案末尾附直播间链接，QQ 客户端会自动识别为可点击；卡片上的链接行改为显示简短域名。
- 新增 3 项测试：图床白名单与 raw 模式、真实取图链路（封面与头像必须内联成功）、可点击链接。

## 1.5.2

- **修复卡片渲染必然失败的问题**：发给文转图服务的 playwright `timeout` 单位写错（毫秒被当成秒再除一次，25 秒配置实际传成 `24`，被 playwright 解释为 24 毫秒），导致服务端每次都在 `Page.goto` 阶段超时并返回 HTTP 500。现在按毫秒传递，并预留 2 秒让服务端先于客户端超时返回自己的错误。
- 新增回归测试：断言传给渲染服务的 timeout 处于毫秒量级且不超过客户端超时，防止再次写错单位。

## 1.5.1

- 修复真实文转图服务渲染 500：封面与头像改为向 B站图床请求缩略图（自动转 WebP），卡片 HTML 载荷从约 330KB 降到约 60–90KB，显著降低 playwright 截图超时与内存压力。缩略图取不到时自动回退原图，不会因此丢封面。
- 新增单张内联图片上限（500KB），避免异常大图把请求体撑爆。
- 新增渲染熔断：连续失败 3 次后暂停卡片渲染 5 分钟，期间通知照常发送、只改发「封面 + 文案」，避免每轮检测都打一次失败请求；WebUI 的「渲染服务自检」不受熔断限制，方便排查。
- 渲染服务返回 4xx/5xx 时，把服务响应正文（截断 200 字）记入运行日志，便于直接定位服务端报错。
- 新增 5 项测试覆盖缩略图、回退、载荷上限、熔断与错误体捕获。

## 1.5.0

- 开播／下播通知改为图片卡片：直播封面、主播头像、直播间二维码、标题、开播时间或本场时长。
- 文案改为《鸣潮》角色爱弥斯第一人称（官方性格描述为「活泼俏皮」），提供爱弥斯／软萌／朴素三种口吻，朴素档保留 1.4.x 的纯文字格式。
- 卡片由外部文转图服务渲染（默认对接 AstrBot 的 astrbot-t2i-service），并兼容返回 JSON 图床地址的服务实现。
- 三级降级：卡片 → 封面 + 文案 → 纯文字；渲染失败不写发送记录、不消耗去重窗口，不会吞掉开播通知。
- 同一条通知只渲染一次，多个群复用同一张图片；卡片按内容哈希缓存在数据目录并按容量与时间清理。
- 新增三套卡片配色与自备贴纸目录 `assets/card/custom/`，贴纸按 `top-`／`tag-`／`corner-` 前缀叠加。
- WebUI 直播页新增「通知卡片」面板：口吻、配色、装饰、服务地址，以及开播／下播卡片预览和渲染服务自检。
- 修复 renderer 日志通道异常会导致手动通知静默不发送的问题，以及手动通知批次变量作用域错误。
- 修复路径包含中文或空格时诊断 CLI 测试无法运行的问题。
- 补充 21 项卡片测试（含本机 mock 文转图服务与降级链），全量 163 项通过。

## 1.4.2

- 未变化的列表跳过重绘，复用日期格式化器，概览时间单独更新。
- 空闲管理页约 15 秒刷新；有任务时约 5 秒；隐藏标签页暂停，错误逐步退避。
- 精简概览、直播和诊断轮询数据，保留完整诊断导出。
- 相同会话避免重复写盘；成功请求明细仅在调试模式输出。
- 修复文件传输中按钮被任务刷新重新启用的问题。
- 142 项本地测试通过；本次源码整理只调整文档、仓库辅助文件与打包入口，运行逻辑不变。

## 1.4.1

- 每天最多 12 个检测时段，支持跨午夜与重叠时段并集，迁移旧单时段设置。
- 手动开播、下播和单群补发改用页内确认，补齐提交反馈与发送日志。

## 1.4.0

- 北京时间检测时段，发现开播后持续跟踪到下播。
- 手动开始／停止检测与恢复自动模式。
- 手动开播／下播通知、单群补发及短时去重。

## 1.3.x

- 增加多主播、多群直播开播／下播通知与发送记录。
- 增加单地址断流重试、备用地址恢复与逐轨重新下载。

## 1.2.x

- WebUI 扫码登录、跨域换票与本地会话保存。
- 普通视频与分 P 下载、480P DASH 选流、FFmpeg 合并与 QQ 视频发送。
- 412／429 冷却、连接自检、下载错误详情与媒体地址兼容。

## 1.0.x–1.1.x

- NapCat 原生 B站识别、封面文字回复、配置、群范围与基础 WebUI。
