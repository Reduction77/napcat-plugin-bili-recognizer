# 源码整理验证

- 运行版本：1.4.2。
- `npm test`：142 项通过，0 失败、0 跳过；包含本机 FFmpeg／ffprobe 合并验证与本地 HTTP 断流恢复测试。
- `index.mjs`、`lib/`、`webui/` 与已交付的 1.4.2 安装包逐字节比对，运行代码未改动。
- `package.json` 保留版本与宿主元数据，增加性能计数和开发打包命令。
- 打包工具实际运行后检查 ZIP 完整性、根目录入口，并对解压后的插件执行入口导入检查。
- 检查 Markdown 本地文档链接、许可证文件和源码包目录结构。

自动测试使用模拟 B站接口、模拟 NapCat 上下文和 Node VM 前端，不代表所有 NapCat 版本或真实 QQ 发送均已验证。测试中的 Cookie、账号与地址是测试样例，源码包不包含用户的实际账号配置或运行数据。

此次仅整理源码和文档，没有创建远程仓库、发布 Release 或推送 GitHub。
